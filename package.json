{
  "name": "pixoo-daemon",
  "version": "1.0.4",
  "main": "daemon.js",
  "scripts": {
    "start": "node daemon.js",
    "build:version": "node scripts/build-version.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier -w .",
    "format:check": "prettier -c .",
    "lint:errors": "eslint . --quiet -f codeframe || true",
    "md:lint": "markdownlint \"**/*.md\" --ignore node_modules --ignore .cursor/rules/cursor-rules.mdc",
    "md:fix": "markdownlint \"**/*.md\" --fix --ignore node_modules --ignore .cursor/rules/cursor-rules.mdc",
    "test": "node --test",
    "coverage": "c8 npm test",
    "hooks:setup": "husky init"
  },
  "dependencies": {
    "mqtt": "^5.9.0",
    "sharp": "^0.33.5",
    "suncalc": "^1.9.0"
  },
  "lint-staged": {
    "*.{js,mjs,cjs,jsx}": [
      "eslint --fix",
      "prettier -w"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier -w"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "eslint": "^9.5.0",
    "eslint-formatter-codeframe": "^7.32.1",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-promise": "^6.2.0",
    "lint-staged": "^15.5.2"
  }
}
